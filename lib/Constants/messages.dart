
// ignore_for_file: constant_identifier_names

import 'package:flutter/material.dart';

const snackBar = SnackBar(
     behavior: SnackBarBehavior.floating,
     margin:EdgeInsets.only(bottom: 50),
     content: Text('Havent reached Destinatoin Yet!'),
     duration: Duration(microseconds: 500000),
   );

const snackBar_inoffice = SnackBar(
     behavior: SnackBarBehavior.floating,
     margin:EdgeInsets.only(bottom: 50),
     content: Text('You are at destination!'),
     duration: Duration(microseconds: 500000),
   );

const snackBar_imagenotselected = SnackBar(
     behavior: SnackBarBehavior.floating,
     margin:EdgeInsets.only(bottom: 50),
     content: Text('Pick Image!'),
     duration: Duration(microseconds: 500000),
   );

const snackBar_LoginError = SnackBar(
     behavior: SnackBarBehavior.floating,
     margin:EdgeInsets.only(bottom: 50),
     content: Text('LoginError!'),
     duration: Duration(microseconds: 500000),
   );

const snackBar_UplaodError = SnackBar(
     behavior: SnackBarBehavior.floating,
     margin:EdgeInsets.only(bottom: 50),
     content: Text('UplaodError!'),
     duration: Duration(microseconds: 500000),
   );

const snackBar_undefined = SnackBar(
     behavior: SnackBarBehavior.floating,
     margin:EdgeInsets.only(bottom: 50),
     content: Text('undefined!'),
     duration: Duration(microseconds: 500000),
   );

const snackBar_nointernetconnection = SnackBar(
     behavior: SnackBarBehavior.floating,
     margin:EdgeInsets.only(bottom: 50),
     content: Text('No Network found'),
     duration: Duration(microseconds: 500000),
   );

const snackBar_wrongotp = SnackBar(
     behavior: SnackBarBehavior.floating,
     margin:EdgeInsets.only(bottom: 50),
     content: Text('Wrong Otp'),
     duration: Duration(microseconds: 500000),
   );

const snackBar_alreadymarked = SnackBar(
     behavior: SnackBarBehavior.floating,
     margin:EdgeInsets.only(bottom: 50),
     content: Text('Already Marked'),
     duration: Duration(microseconds: 500000),
   );